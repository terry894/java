package day8Hw;

public class Hw0906no3and4 {
// object: 모든 클래스의 최상위 클래스
	// 11개의 메소드를 가지고 있다
	// getclass: 객체의 클래스형을 반환한다.	
	// hashcode: 객체의 코드값을 반환한다.
	// equals: 2객체가 같은지 비교하여 같으면 true, 다르면 false
	// clone: 객체를 복사한다
	// toString: 현재 객체의 문자열을 반환한다
	// notify: wait된 스레드 실행을 재개할 때 호출한다
	// notifyAll: wait된 모든 스레드 실행을 재개할 때 호출한다.
	// wait, wait(long timeout) , wait(long timeout, int nanos) 
	// finalize: 가비지 컬렉션 직전에 객체의 리소스를 정리할 때 호출한다.

	
}
